# Campus Music - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection URL
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Individual PostgreSQL connection parameters (optional if DATABASE_URL is set)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=campus_music

# ===========================================
# SESSION & SECURITY (Required)
# ===========================================
# Secret key for session encryption (generate a random 32+ character string)
SESSION_SECRET=your-secure-session-secret-key-here

# Node environment (development | production)
NODE_ENV=development

# ===========================================
# STRIPE PAYMENTS (Required for tips feature)
# ===========================================
# Stripe API keys from dashboard.stripe.com
# Note: On Replit, use the built-in Stripe integration instead
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# FILE STORAGE (Required for uploads)
# ===========================================
# Google Cloud Storage / Replit Object Storage
# On Replit, these are auto-configured via the Object Storage integration
# For other platforms, configure GCS credentials
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ===========================================
# EMAIL (Optional - for future email verification)
# ===========================================
# SMTP settings for sending emails
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# SMTP_FROM=noreply@campusmusic.com

# ===========================================
# DEPLOYMENT (Set by hosting platform)
# ===========================================
# Server port (default: 5000)
PORT=5000

# Base URL for the application (used for Stripe redirects, etc.)
# Required for non-Replit deployments
APP_URL=https://your-app-domain.com

# ===========================================
# REPLIT-SPECIFIC (Auto-configured on Replit)
# ===========================================
# These are automatically set when running on Replit.
# For deployment on other platforms, you must modify the code - see README.md.

# --- Replit Environment Variables (auto-set on Replit) ---
# REPLIT_DOMAINS - Used for generating public URLs in server/routes.ts and server/app.ts
# REPLIT_CONNECTORS_HOSTNAME - Used by Stripe connector in server/stripeClient.ts
# REPL_IDENTITY / WEB_REPL_RENEWAL - Used for X_REPLIT_TOKEN auth in server/stripeClient.ts
# REPLIT_DEPLOYMENT - Used to detect production mode in server/stripeClient.ts

# --- Replit Object Storage ---
# The object storage uses Replit sidecar on port 1106 (server/objectStorage.ts)
# For non-Replit deployments, replace the implementation with standard GCS SDK

# --- For Non-Replit Deployments ---
# You must modify these files to work outside Replit:
# 1. server/stripeClient.ts - Replace with standard Stripe SDK initialization
# 2. server/objectStorage.ts - Replace with @google-cloud/storage SDK
# 3. server/routes.ts & server/app.ts - Use APP_URL instead of REPLIT_DOMAINS

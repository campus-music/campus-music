# Campus Music - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection URL - this is the primary database connection
# On Render, this is automatically set from the linked PostgreSQL database
DATABASE_URL=postgresql://username:password@host:5432/database_name

# ===========================================
# SESSION & SECURITY (Required)
# ===========================================
# Secret key for session encryption (generate a random 32+ character string)
# On Render, this can be auto-generated using generateValue: true
SESSION_SECRET=your-secure-session-secret-key-here

# ===========================================
# STRIPE PAYMENTS (Required for tips feature)
# ===========================================
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Webhook secret for processing Stripe events
# Get this when creating a webhook endpoint in Stripe Dashboard
# Endpoint URL should be: https://your-app-url.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# S3-COMPATIBLE STORAGE (Required for uploads)
# ===========================================
# Works with AWS S3, DigitalOcean Spaces, MinIO, Backblaze B2, etc.
# If not configured, falls back to local filesystem storage (development only)

# Your S3 bucket name
S3_BUCKET_NAME=your-bucket-name

# AWS Access Key ID (or equivalent for other S3-compatible providers)
S3_ACCESS_KEY=your-access-key

# AWS Secret Access Key (or equivalent for other S3-compatible providers)
S3_SECRET_KEY=your-secret-key

# AWS region (e.g., us-east-1, us-west-2, eu-west-1)
S3_REGION=us-east-1

# Optional: Custom S3 endpoint for non-AWS providers
# Examples:
#   DigitalOcean Spaces: https://nyc3.digitaloceanspaces.com
#   MinIO: https://minio.your-domain.com
#   Backblaze B2: https://s3.us-west-002.backblazeb2.com
# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com

# ===========================================
# DEPLOYMENT (Required for production)
# ===========================================
# Server port (default: 5000)
PORT=5000

# Base URL for the application (used for Stripe redirects, OAuth callbacks, etc.)
# Must NOT have a trailing slash
# Examples:
#   https://campus-music.onrender.com
#   https://your-custom-domain.com
APP_URL=https://your-app-domain.com
